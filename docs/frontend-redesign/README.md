# Datus Agent — 前端 UI 重构设计文档

> 版本: v1.0  
> 日期: 2026-08-24  
> 状态: 设计阶段 / 待评审

---

## 目录

1. [设计理念与设计系统](#1-设计理念与设计系统)
2. [页面级重构方案](#2-页面级重构方案)
3. [交互模式与组件规范](#3-交互模式与组件规范)
4. [实施路线图](#4-实施路线图)
5. [附录: 参考文章核心观点映射](#5-附录)

---

## 1. 设计理念与设计系统

### 1.1 产品定位

Datus 不是一个普通的聊天机器人,而是一个**数据智能工作台 (Data Workbench)**。用户来这里的目的是:

- 用自然语言与数据对话
- 让 Agent 辅助完成数据分析、数据库查询、数据采集等任务
- 获得可信任的、可追溯的分析结果

核心设计原则: **精密、可信、透明**

### 1.2 设计语言: 精密仪器

从三个参考意象中提取设计语言:

| 意象 | 提取的设计元素 |
|------|----------------|
| **金融数据终端** (Bloomberg Terminal) | 深色画布、高信息密度、精确的排版、色彩只用于数据本身 |
| **IDE 工作台** (VS Code / JetBrains) | 可折叠面板、上下文感知的状态栏、命令面板、键盘优先 |
| **实验室控制台** | 状态指示灯、进度追踪、可暂停/干预的工作流 |

### 1.3 设计 Token 系统

#### 色板

```css
/* 主画布 — 深色,减少视觉疲劳,让数据发光 */
--bg-canvas:       #0f172a;   /* 主背景,深海军蓝 */
--bg-surface:      #1e293b;   /* 面板/卡片表面 */
--bg-elevated:     #334155;   /* 悬浮/模态/下拉层 */
--bg-hover:        rgba(255, 255, 255, 0.05);

/* 主色 — 青金石蓝,代表数据与智能的融合 */
--accent:          #06b6d4;   /* 主色调 */
--accent-soft:     rgba(6, 182, 212, 0.12);
--accent-glow:     rgba(6, 182, 212, 0.25);

/* 语义色 — 只用关键信息 */
--amber:           #f59e0b;   /* 数据高亮/指标标记 */
--emerald:         #10b981;   /* 成功/完成 */
--danger:          #ef4444;   /* 错误/中断 */
--info:            #3b82f6;   /* 信息/提示 */

/* 文字 — 高对比度,保证可读性 */
--text-primary:    #f1f5f9;
--text-secondary:  #94a3b8;
--text-muted:      #64748b;

/* 边框 */
--border:          rgba(148, 163, 184, 0.12);
--border-hover:    rgba(148, 163, 184, 0.25);
```

#### 字体

| 角色 | 字体 | 权重 | 说明 |
|------|------|------|------|
| UI / 正文 | Inter | 400 / 500 / 600 / 700 | 数据密集型 UI 首选,精密克制 |
| 数据 / 代码 | JetBrains Mono | 400 / 500 / 600 | 等宽,适合 SQL、代码、指标 |
| 展示标题 | Plus Jakarta Sans | 600 / 700 / 800 | 几何感,用于大标题和品牌文字 |

> 注意: Plus Jakarta Sans 需要 npm 安装 (`@fontsource/plus-jakarta-sans`),也可以回退到 Inter 的 display 权重。

#### 间距与圆角

沿用 4→8→16→24→32→48 体系,但调整圆角:

| 层级 | 圆角 | 适用 |
|------|------|------|
| 最小元素 | 4px | 标签、徽章、小图标 |
| 卡片/面板 | 8px | 卡片、侧边栏、面板 |
| 输入框/模态 | 12px | 输入框、对话框、大容器 |
| 按钮 | 6px | 所有按钮 |
| 圆形 | 9999px | 头像、状态点 |

#### 阴影

```css
--shadow-sm:   0 1px 3px rgba(0,0,0,0.3);
--shadow-md:   0 4px 12px rgba(0,0,0,0.35);
--shadow-lg:   0 8px 30px rgba(0,0,0,0.4);
--shadow-accent: 0 0 0 3px var(--accent-glow);
```

### 1.4 图标系统

**现状**: 导航和任务类型使用 emoji 图标 (🏠 ✚ 🔍 📜 等)

**改进**: 使用 **SVG 线框图标集** (推荐 Lucide 或 Heroicons)

- 统一 20px 视口 (导航) / 24px (卡片)
- 描边宽度 1.5px
- 颜色继承 `currentColor`
- 交互状态: hover 时描边加粗或填充

导航图标映射:

| 页面 | 当前 emoji | 替换图标 (Lucide) |
|------|-----------|-------------------|
| 首页 | 🏠 | LayoutDashboard |
| 新建任务 | ✚ | Plus |
| 数据探索 | 🔍 | Search |
| 会话历史 | 📜 | History |
| 数据源 | 🔌 | Cable |
| 技能市场 | 🛍️ | Puzzle |
| 知识库 | 📚 | Library |
| 设置 | ⚙️ | Settings |

---

## 2. 页面级重构方案

### 2.1 整体布局

```
┌─────────────────────────────────────────────────────────┐
│ ┌──────────┐  ┌──────────────────────────────────────┐  │
│ │          │  │                                      │  │
│ │ Sidebar  │  │          Main Content Area           │  │
│ │ (折叠式)  │  │                                      │  │
│ │          │  │  Dashboard / Chat / Settings / ...   │  │
│ │ 导航图标  │  │                                      │  │
│ │ 会话列表  │  │                                      │  │
│ │          │  │                                      │  │
│ │ 用户设置  │  │                                      │  │
│ └──────────┘  └──────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

**关键变化**:
- 侧边栏支持折叠 (展开 224px, 折叠 56px 只显示图标)
- 所有页面共用此布局,由 ChatLayout 统一管理
- 主内容区域根据路由切换页面

### 2.2 侧边栏 (Sidebar)

**现状问题**:
- emoji 图标不够精致
- 会话列表和导航混在一起
- 缺少搜索和固定功能

**重构方案**:

```
┌──────────────────┐
│  ◆  Datus        │  ← 品牌 Logo + 名称
│                  │
│  ◇ 首页          │  ← SVG 线框图标
│  ＋ 新建任务      │
│  ◇ 数据探索      │
│  ◇ 会话历史      │
│ ───────────────── │
│  ◇ 数据源        │
│  ◇ 技能市场      │
│  ◇ 知识库        │
│  ◇ 设置          │
│ ───────────────── │
│  最近会话         │
│  [搜索会话...]    │  ← 新增搜索
│  ○ 销售分析...    │
│  ○ 用户留存...    │
│  ○ GMV 归因...    │
│ ───────────────── │
│  ◎ User        ⚙️ │  ← 用户+设置
└──────────────────┘
```

**具体规范**:
- 导航项高度: 36px, 间距 2px
- 激活态: 左侧 2px 青色竖条 + 浅青背景
- 会话列表: 最多显示 10 条,支持搜索过滤
- 折叠态: 只显示图标, hover 展开 tooltip 显示名称
- "新建对话"按钮改为侧边栏顶部的 `＋` 图标按钮

### 2.3 首页 (Dashboard)

**现状问题**:
- 6 张 emoji 卡片平铺,看起来像通用后台模板
- 系统状态和功能入口混在一起
- 缺少"智能感"

**重构方案**:

```
┌──────────────────────────────────────────────────────┐
│  Datus Agent                                    ⚙️  │
│  用自然语言与数据对话                                  │
├──────────────────────────────────────────────────────┤
│ ┌────────────┐ ┌────────────┐ ┌────────────┐        │
│ │ ▓ 数据源    │ │ ▓ Agent    │ │ ▓ 知识库    │        │
│ │ 3 个已连接  │ │ 2 个可用   │ │ 已构建      │        │
│ │ 最后同步:.. │ │ 数据分析师  │ │ 12 个主题   │        │
│ └────────────┘ └────────────┘ └────────────┘        │
├──────────────────────────────────────────────────────┤
│ 快速开始                                             │
│ ┌──────────┐ ┌──────────┐ ┌──────────┐              │
│ │ 对话查询  │ │ 数据库问数│ │ 数据采集  │              │
│ │ 自然语言  │ │ 选表查数  │ │ URL 采集  │              │
│ │ 分析数据  │ │ 生成报告  │ │ 结构化   │              │
│ └──────────┘ └──────────┘ └──────────┘              │
├──────────────────────────────────────────────────────┤
│ 试试这样问                                           │
│ [统计近30天各品类销售额] [找出客户流失原因]            │
│ [生成月度经营分析报告] [分析最近一周异常订单]          │
└──────────────────────────────────────────────────────┘
```

**具体规范**:
- 顶部状态卡片: 3 个大卡片,显示关键系统状态,点击跳转到对应配置页
- 快速开始: 3 个核心场景,替代原来的 6 个功能入口
- 示例 prompt: 标签样式,点击直接跳转到聊天页并填充对应任务类型
- 去掉"技能市场"和"会话历史"的首页入口 (它们属于侧边栏导航)
- 整体信息密度降低,但数据密度提高

### 2.4 聊天页 (ChatPage) — 核心页面

**这是重构的重点,分三个状态描述**

#### 状态 A: 初始状态 (无消息)

**现状问题**: TaskPanel + BuiltInSkillsPanel + ChatArea 三个面板同时出现,信息过载

**重构方案**:

```
┌──────────────────────────────────────────────────────┐
│ [数据分析] [数据库问数] [数据采集]     ← 频道标签    │
├──────────────────────────────────────────────────────┤
│                                                      │
│                    你想分析什么?                      │
│           上传表格或关联数据源,我来做清洗、           │
│           建模、可视化,并写出结论。                   │
│                                                      │
│  ┌──────────────────────────────────────────────┐   │
│  │ 分析 2025 年中国新能源汽车销量数据...  [↑]    │   │
│  └──────────────────────────────────────────────┘   │
│   📎 上传文件      📋 分析深度: 标准 ▼  输出格式 ▼  │
│                                                      │
│  快速开始                                            │
│  ┌──────────────────┐ ┌──────────────────┐          │
│  │ 销售趋势与同环比  │ │ 客户分群与复购   │          │
│  │ 6 张图表          │ │ 建模             │          │
│  └──────────────────┘ └──────────────────┘          │
│                                                      │
└──────────────────────────────────────────────────────┘
```

**关键变化**:
- 任务类型选择从左侧面板改为顶部的**频道标签** (3 个标签切换)
- 输入框居中,视觉重心突出
- 文件上传、输出选项折叠在输入框下方一行
- 快速操作卡片减少到 2 个,更聚焦

#### 状态 B: 对话中

**现状问题**: 消息列表 + 输入框,无 Agent 状态展示,无上下文提示

**重构方案**:

```
┌──────────────────────────────────────────────────────┐
│ 🤖 数据分析师  │  💾 sales_db  │  🧠 GPT-4o  │  📋 规划 │
│  ← 上下文条: 显示当前 Agent/DB/Model/模式              │
├──────────────────────────────────────────────────────┤
│                                                      │
│ 💬 用户: 分析近12个月销售趋势                      │
│                                                      │
│ 🤖 Datus                                             │
│ ┌──────────────────────────────────────────────┐    │
│ │ ● 理解需求: 销售趋势分析,12个月,同比环比  ✅  │    │
│ │ ● 查询数据: sales_db.orders (21,304 rows) ✅  │    │
│ │ ● 生成图表: 月趋势线图 + 同比热力图     ◌     │    │
│ │ ● 撰写结论: 等待...                      ☐     │    │
│ └──────────────────────────────────────────────┘    │
│  [暂停]                                              │
│                                                      │
│  ## 销售趋势分析报告                                 │
│                                                      │
│  ### 总体趋势                                       │
│  近12个月总销售额 ¥12,345 万,同比增长 18.5%...      │
│                                                      │
│  | 月份 | 销售额 | 同比 | 环比 |                     │
│  |------|--------|------|------|                     │
│  | 1月  | 890万  | +12% | -8%  |                     │
│  ...                                                 │
│                                                      │
│  📊 [趋势图]  [同比热力图]                           │
│                                                      │
│  ⏱ 生成耗时 12s  [复制] [重新生成] [继续追问]       │
├──────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────┐   │
│  │ 继续追问或输入新的问题...              [↑]    │   │
│  └──────────────────────────────────────────────┘   │
│  正在使用 sales_db · 数据分析师 · GPT-4o            │
└──────────────────────────────────────────────────────┘
```

**关键变化**:
1. **上下文条 (Context Bar)**: 聊天区域顶部,显示当前 Agent/DB/Model/规划模式,以标签形式展示,点击弹出选择器
2. **Agent 思考过程轨道**: 消息气泡上方显示可折叠的步骤列表,包含状态图标、步骤名称、耗时
3. **暂停按钮**: 消息区域顶部,支持暂停/继续
4. **消息操作按钮**: 复制、重新生成、继续追问
5. **输入框上下文**: 输入框下方显示当前上下文摘要

#### 状态 C: 流式输出中

- 思考过程轨道实时更新,当前步骤显示脉冲动画
- 步骤状态: 待执行(灰色☐) → 执行中(青色脉冲 ◌) → 完成(绿色 ✅) → 失败(红色 ❌)
- 工具调用显示为可折叠卡片,包含调用参数和返回摘要
- 流式文本显示打字机效果 + 光标闪烁

### 2.5 设置页 (SettingsPage)

**现状问题**: 标签页切换在顶部,内容区域平铺

**重构方案**:

```
┌──────────────────────────────────────────────┐
│ 设置                                          │
│ 管理模型、数据源和系统配置                     │
├───────────┬──────────────────────────────────┤
│ 模型      │ ┌──────────────────────────────┐ │
│ 数据源    │ │ 模型名称          状态  操作  │ │
│ 通用      │ │ GPT-4o            ● 已连接  │ │
│           │ │ Claude 3.5 Sonnet ● 已连接  │ │
│           │ │ ...                         │ │
│           │ │ [添加模型]                   │ │
│           │ └──────────────────────────────┘ │
└───────────┴──────────────────────────────────┘
```

**关键变化**:
- 左侧导航式标签,类似系统设置
- 每个配置项显示更详细的状态信息
- 支持测试连接、编辑、删除操作

### 2.6 知识库页 (KnowledgeBasePage)

**现状问题**: 主题卡片信息少,搜索体验平淡

**重构方案**:
- 主题卡片增加: 文档数量、最后更新时间、构建状态
- 搜索高亮关键词
- 构建流程: 选择数据源 → 选择文档 → 确认 → 进度条 → 完成
- 增加"引用此知识库"开关,在聊天上下文中显示

### 2.7 技能市场页 (SkillShopPage)

**现状问题**: 布局可优化,搜索和筛选可增强

**重构方案**:
- 三列等宽卡片网格
- 卡片包含: 图标、名称、描述、标签、版本、安装状态、操作按钮
- 详情面板: 点击卡片后右侧滑入,不覆盖列表
- 搜索 + 分类筛选 (内置/已安装/全部)

---

## 3. 交互模式与组件规范

### 3.1 Agent 思考过程组件 (ThinkingProcess)

**这是本次重构最重要的新组件**

```
Props:
- steps: AgentStep[]  (id, label, status, detail?, duration?)
- currentStep: string | null
- onToggleExpand: (stepId) => void
- onPause: () => void
- onResume: () => void
- onStop: () => void
- collapsed: boolean   (默认展开最后一步,其余折叠)

状态机:
  pending → processing → completed
                       → failed
                       → paused (仅当前步骤)

视觉:
- 垂直时间线布局
- 每个步骤左侧有状态点 (灰色/青色脉冲/绿色/红色)
- 步骤间用细线连接
- 当前步骤: 高亮 + 脉冲动画
- 已完成步骤: 可点击展开查看详情
- 工具调用步骤: 显示为代码块样式卡片
```

### 3.2 上下文条 (ContextBar)

```
Props:
- agent: { id, name, icon } | null
- datasource: { name, type } | null
- model: { id, name, provider } | null
- planMode: boolean
- onAgentChange, onDatasourceChange, onModelChange, onPlanModeToggle

视觉:
- 单行,标签样式 (pill/badge)
- 每个标签: 图标 + 名称 + 下拉箭头
- 点击弹出选择器浮层 (而非下拉框)
- 规划模式: 独立的切换按钮,带状态指示
- 背景: 半透明,融入消息区域顶部
```

### 3.3 消息气泡增强 (MessageBubble)

**现状**: 简单的用户/助理气泡,无操作

**改进**:
- 用户消息: hover 出现编辑按钮,点击进入编辑模式
- 助理消息:
  - 消息头部: Agent 头像 + 名称 + 模型名 + 耗时
  - 思考过程: 嵌入 ThinkingProcess 组件
  - 结果正文: Markdown 渲染增强
  - 消息底部: 操作按钮组 (复制/重新生成/继续追问)
- 流式消息: 显示打字机光标 + 思考过程实时更新

### 3.4 输入框增强 (InputBox)

**现状**: 纯文本输入 + 发送按钮 + 工具栏

**改进**:
- 输入框: 自动调整高度,支持 `/` 命令触发技能自动补全
- 左侧 `+` 按钮: 展开菜单 (上传文件、选择技能、引用知识库)
- 输入框下方: 上下文摘要条 ("正在使用 sales_db · 数据分析师 · GPT-4o")
- 底部: 字数统计 + 快捷键提示 (Enter 发送, Shift+Enter 换行)
- 发送按钮: 流式时显示为暂停图标,可点击暂停

### 3.5 暂停-反馈-继续机制

**新增交互模式**,对应参考文章中的模式 6:

```
触发时机:
1. 用户手动点击暂停按钮
2. Agent 到达关键决策点 (如: 需要确认 API 调用参数)
3. Agent 检测到需要用户输入

暂停状态:
- 暂停按钮变为继续按钮
- 显示"已暂停"指示器
- 提供选项: 继续 / 修改输入 / 中止并保存
- 修改输入: 打开编辑面板,用户可调整指令后继续

视觉:
- 暂停: 两个竖线图标 ⏸
- 继续: 三角形图标 ▶
- 暂停状态: 消息区域顶部显示黄色横幅
```

### 3.6 快捷键系统

| 快捷键 | 动作 |
|--------|------|
| `Enter` | 发送消息 |
| `Shift+Enter` | 换行 |
| `⌘/Ctrl+K` | 打开命令面板 |
| `⌘/Ctrl+N` | 新建对话 |
| `⌘/Ctrl+,` | 打开设置 |
| `⌘/Ctrl+Shift+P` | 切换规划模式 |
| `Esc` | 关闭浮层/模态 |
| `↑` | 在输入框编辑上一条消息 |
| `/` | 触发技能自动补全 |

### 3.7 响应式断点

| 断点 | 布局变化 |
|------|----------|
| ≥ 1024px | 完整三栏布局 |
| 768-1023px | 侧边栏自动折叠,上下文条缩短 |
| < 768px | 侧边栏收起为抽屉,上下文条隐藏为图标,输入框全宽 |

---

## 4. 实施路线图

### Phase 1: 设计系统 + 基础布局 (预计 1-2 天)

**目标**: 建立新的设计系统,重构全局布局

**任务清单**:
- [ ] 更新 globals.css: 新色板、字体、圆角、阴影
- [ ] 安装 Plus Jakarta Sans 字体
- [ ] 创建 SVG 图标组件库 (或集成 Lucide React)
- [ ] 侧边栏重构: 可折叠、SVG 图标、搜索功能
- [ ] 聊天布局重构: 移除 TaskPanel 和 BuiltInSkillsPanel,改为频道标签
- [ ] 更新全局样式: 滚动条、焦点态、过渡动画

### Phase 2: 核心交互改进 (预计 2-3 天)

**目标**: 实现 Agent 思考可视化和上下文条

**任务清单**:
- [ ] 创建 ThinkingProcess 组件 (步骤时间线、状态机、展开/折叠)
- [ ] 创建 ContextBar 组件 (标签选择器、点击弹出浮层)
- [ ] 重构 Toolbar: 从 dock 模式改为 ContextBar
- [ ] 实现暂停/继续/中止机制
- [ ] 增强消息气泡: 操作按钮、编辑模式、思考过程嵌入
- [ ] 流式输出时思考过程实时更新

### Phase 3: 页面打磨 (预计 2-3 天)

**目标**: 重构所有独立页面

**任务清单**:
- [ ] Dashboard 重设计: 指挥中心布局
- [ ] 输入框增强: `/` 命令、`+` 菜单、上下文摘要
- [ ] 设置页重构: 左侧导航标签
- [ ] 知识库页改进: 搜索高亮、构建流程
- [ ] 技能市场页改进: 三列网格、详情滑入面板
- [ ] 会话历史页增强

### Phase 4: 细节与响应式 (预计 1-2 天)

**目标**: 微交互动画、响应式适配、无障碍

**任务清单**:
- [ ] 响应式断点适配 (平板/手机)
- [ ] 微交互: 卡片 hover、页面切换过渡、加载骨架屏
- [ ] 键盘快捷键支持
- [ ] 无障碍: 焦点管理、ARIA 标签、角色
- [ ] 暗色/亮色主题切换 (可选)
- [ ] 性能优化: 虚拟列表、懒加载

---

## 5. 附录

### 5.1 参考文章: AI Agent HCI 核心模式映射

| 模式 | 设计文档中的对应设计 |
|------|---------------------|
| 注意力引导 | 频道标签、折叠面板、渐进式展示、思考过程默认折叠 |
| 就地澄清 | 消息可编辑、输入框 `/` 命令、结果上直接修改 |
| 自动建议 | 快速操作卡片、`/` 命令补全、关键决策点选项 |
| 思考外显 | ThinkingProcess 组件、步骤时间线、工具调用卡片 |
| 上下文匹配 | ContextBar、输入框上下文摘要、知识库自动关联 |
| 暂停-反馈-继续 | 暂停按钮、关键节点自动暂停、修改/继续/中止选项 |
| 环境适配 | 响应式布局、结果导出、快捷键系统 |

### 5.2 现有文件清单

重构涉及的主要文件:

```
frontend/src/
├── App.tsx                      # 路由入口 (保持)
├── layouts/ChatLayout.tsx       # 整体布局 (修改)
├── components/
│   ├── Sidebar.tsx              # 侧边栏 (重构)
│   ├── ChatArea.tsx             # 聊天区域 (重构)
│   ├── InputBox.tsx             # 输入框 (增强)
│   ├── MessageBubble.tsx        # 消息气泡 (增强)
│   ├── Toolbar.tsx              # 工具栏 (改为 ContextBar)
│   ├── TaskPanel.tsx            # 任务面板 (移除/合并)
│   ├── BuiltInSkillsPanel.tsx   # 内置技能面板 (移除/合并)
│   ├── MarkdownRenderer.tsx     # Markdown 渲染 (增强)
│   ├── FileUpload.tsx           # 文件上传 (保持)
│   ├── OutputOptions.tsx        # 输出选项 (保持)
│   ├── TemplateCard.tsx         # 模板卡片 (保持)
│   ├── AddModelModal.tsx        # 添加模型 (保持)
│   ├── AddDatasourceModal.tsx   # 添加数据源 (保持)
│   ├── CreateSkillModal.tsx     # 创建技能 (保持)
│   └── ... (新组件)
│       ├── ThinkingProcess.tsx   # 新增: 思考过程
│       ├── ContextBar.tsx       # 新增: 上下文条
│       └── Icons.tsx            # 新增: SVG 图标
├── pages/
│   ├── DashboardPage.tsx        # 首页 (重构)
│   ├── ChatPage.tsx             # 聊天页 (重构)
│   ├── SettingsPage.tsx         # 设置页 (重构)
│   ├── KnowledgeBasePage.tsx    # 知识库 (改进)
│   ├── SkillShopPage.tsx        # 技能市场 (改进)
│   ├── SessionsPage.tsx         # 会话历史 (改进)
│   ├── DataConnectionPage.tsx   # 数据源 (保持)
│   └── DataExplorerPage.tsx     # 数据探索 (保持)
├── stores/chatStore.ts          # 状态管理 (扩展)
├── styles/globals.css           # 全局样式 (重构)
└── hooks/                       # 现有 hooks (保持)
```

### 5.3 现有设计系统 vs 新设计系统对比

| 维度 | 当前 | 新方案 |
|------|------|--------|
| 主背景 | #f1f5f9 (浅灰) | #0f172a (深海军蓝) |
| 主色 | #0891b2 (青) | #06b6d4 (青金石蓝) |
| 侧边栏背景 | #0b1120 | #0f172a (统一) |
| 导航图标 | emoji | SVG 线框图标 |
| 任务选择 | 左侧面板 | 顶部频道标签 |
| 工具栏 | 输入框内嵌 dock | 独立上下文条 |
| Agent 状态 | "思考中..." 三个点 | 步骤时间线 + 状态机 |
| 暂停/继续 | 无 | 暂停按钮 + 关键节点自动暂停 |
| 信息密度 | 平铺所有信息 | 分层/折叠/渐进式展示 |
| 响应式 | 无 | 三档断点适配 |

---

> 此文档是 UI 重构的完整蓝图。评审通过后,按照 Phase 1→4 的顺序逐步实施。
> 每个 Phase 完成后提交 PR 并验证,确保渐进式交付价值。